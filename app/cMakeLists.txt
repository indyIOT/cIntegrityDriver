add_executable(${EXECUTABLE_NAME}  source/main.c)

target_include_directories(${EXECUTABLE_NAME} PRIVATE include)
target_include_directories(${EXECUTABLE_NAME}  PRIVATE ../../cCommonMacros/include )
target_include_directories(${EXECUTABLE_NAME}  PRIVATE ../../cCommonTypes/include )
target_include_directories(${EXECUTABLE_NAME} PRIVATE ../cIntegrityDriverLib/publicInclude)


target_link_libraries(${EXECUTABLE_NAME} PUBLIC ${INTEGRITY_DRIVER_LIBRARY_NAME} )

